name: Create Multiple Repositories with Workflows

on:
  workflow_dispatch:  # Allows manual trigger

jobs:
  create_repos:
    runs-on: ubuntu-latest

    steps:
      - name: Setup Git Config
        run: |
          git config --global user.email "lalittanysudhakar@gmail.com"
          git config --global user.name "lalitsudhakar"

      - name: Create Repositories & Add Workflows
        env:
          GH_PAT: ${{ secrets.GH_PAT }}  # Use PAT with repo permissions
          ORG_NAME: "lalitsudhakar"
        run: |
          for i in $(seq 1 5); do  # Creates 5 repositories (Modify as needed)
            REPO_NAME="dummy-repo-$i"
            echo "Creating repository: $REPO_NAME"

            # Create repository with error handling
            RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" -X POST \
              -H "Authorization: token $GH_PAT" \
              -H "Accept: application/vnd.github.v3+json" \
              https://api.github.com/user/repos \
              -d "{\"name\": \"$REPO_NAME\", \"private\": false}")

            if [[ "$RESPONSE" != "201" ]]; then
              echo "❌ Failed to create repo: $REPO_NAME (HTTP $RESPONSE)"
              continue  # Skip to the next repo
            fi

            sleep 2

            # Clone the repository
            git clone https://$GH_PAT@github.com/$ORG_NAME/$REPO_NAME.git
            cd $REPO_NAME

            # Add a dummy Python script
            echo 'print("Hello from Dummy Repo")' > main.py
            git add main.py
            git commit -m "Add dummy Python script"
            git push origin main

            # Create GitHub Actions workflow directory
            mkdir -p .github/workflows

            # Create and push branches with their own workflows
            for branch in "main" "Test-Github" "Docker-Dev" "Prod" "Stage"; do
              git checkout -b $branch
              git push origin $branch

              # Define branch-specific workflow
              echo "name: CI for $REPO_NAME ($branch)

on:
  push:
    branches:
      - $branch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Run Script
        run: echo \"Building $REPO_NAME on branch $branch\"
" > .github/workflows/main-$branch.yml

              git add .github/workflows/main-$branch.yml
              git commit -m "Added CI workflow for $REPO_NAME ($branch)"
              git push origin $branch
            done

            cd ..
            rm -rf $REPO_NAME  # Clean up local repo
          done
